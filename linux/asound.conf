# The snap's ALSA default goes to the session sound server through the pulse
# plugin staged beside it (libasound2-plugins); the raw alsa interface is
# deliberately not plugged.
pcm.!default { type pulse }
ctl.!default { type pulse }
