# Linux stores — Flathub, the Snap Store and AppImage

The prepared Flathub, Snap Store and AppImageHub listings, kept here the way
`docs/WINDOWS_STORE_LISTING.md` keeps the Windows one: every field, its
value, and the reasoning where a reviewer will want it. The machine-readable
source is `linux/listing.toml`; `go run ./cmd/linuxmeta render` turns it into
the AppStream MetaInfo, the desktop entries, the icons, `snap/snapcraft.yaml`
and the Flatpak manifest, and the tests in `cmd/linuxmeta` hold the committed
files equal to a fresh render. Edit the source, never the outputs.

State on 19 September 2026: everything below exists in the tree; the three
packages have been built and smoked on Linux runners, the Snap Store account
and name exist and the publishing credential is in place, and the first
packaged version is the next tag (the
published 1.2.9 tarball predates the `bibletext:` scheme handler and the
single-instance code, and one version names one tree on every channel).

## Decisions taken, and the ones still open

| Question | Decision | Why |
| --- | --- | --- |
| App id | `uk.co.bibletext.BibleText` on Flatpak, the snap's metainfo and the AppImage; the tarball keeps `uk.co.bibletext` and Fyne's runtime id stays `bibletext` | Flathub's shape is tld.vendor.product and the four-part form verifies to bibletext.co.uk under both the website's and the linter's rules; a reader moving from the tarball to a store build starts with a fresh profile |
| Executable | `bibletext` (the tarball keeps `desktop`) | |
| Runtime / base | org.freedesktop.Platform 25.08 with the golang SDK extension; core24 with the gnome extension; the static type-2 AppImage runtime 20251108 with appimagetool 1.9.1 | 24.08 reached end of life on 8 September 2026; the golang extension has no 26.08 branch yet — bump when it does |
| Flathub's NKJV | **Keyless.** Flathub builds from source on its own machines with no secrets, so the Flathub build carries no API.Bible key; the reader adds a free key under Settings, and the Flathub metainfo says so | `docs/API_KEY_HANDLING.md` forbids the encoded key anywhere but the secret and the final binaries, and the flathub organisation's repository is not ours. The snap and the AppImage are built in our own Actions and carry the key like the tarball |
| Go modules offline | `flatpak/go.mod.yml` and `flatpak/modules.txt`, generated by flatpak-go-mod from the stock `go.mod`; the five toolkit patches are applied to the vendored module in the build commands | flatpak-go-mod emits nothing for a local `replace`, so the tracked `patches/` stay the single truth; `-mod=vendor` never checksums vendored content |
| Snap binary | The release job builds and verifies the keyed executable once and stages it; `snapcraft.yaml` uses the dump plugin | snapcraft builds inside LXD with no repository secrets; one binary, packaged three ways |
| Snap audio | `libasound2-plugins` staged with a layout and `/etc/asound.conf` (`type pulse`) | the audio library is ALSA; the gnome platform snap has no pulse plugin; the raw `alsa` interface is not auto-connected |
| AppImage name | `BibleText-x86_64.AppImage` plus `.zsync`, constant | the site links `/releases/latest/download/` by name; the version travels inside as `X-AppImage-Version` |
| AppImage and `bibletext:` links | Not registered by the AppImage itself; a reader pastes the link into Search | the integration tools either cannot run static-runtime images or strip `%u` (backlog) |
| glibc floor | `scripts/check-glibc-floor.sh` holds the executable at or below GLIBC 2.35 (Ubuntu 22.04) | the runner is pinned to ubuntu-24.04; the 1.2.9 binary's highest symbol was 2.34 |
| Architectures | **x86_64 and arm64.** The tarball and the snap ship both. The AppImage stays x86_64 until `scripts/build-appimage.sh` has an arm64 appimagetool and runtime pinned by sha256 like its x86_64 pair. `flatpak/flathub.json` names x86_64 until the `aarch64` leg of `linux-stores.yml` has gone green — the manifest is built there for both, so that line is a pending proof, not a policy. | The former reason read "the toolkit selects GLES on arm64 and the repository has never built or smoked Linux/arm64" — which was a PROOF gap recorded as an architecture exclusion, so it read as settled rather than untested. Both halves were answered on 18 September 2026 on an arm64 machine: the executable builds clean with no new patches and passes `scripts/smoke-linux-launch.sh` (launch, `bibletext:` handoff, single instance); the snap packs in LXD, installs, exports its desktop entry with `%u`, and its ALSA layout resolves **inside the confinement**. That last one is why this mattered: the layout binds a real path, so an arm64 snap carrying the x86_64 triplet builds, installs and launches perfectly and has no narration audio. |
| OARS | every attribute none | no chat, purchases, ads, location or accounts, and no information about the reader reaches us; Scripture and narration come from their providers, and the AI feature sends the reader's own questions to the provider the reader chose under the reader's own account |
| Summary | `Read and study Scripture` | Flathub: at most 35 characters, no full stop, no article; the Play short description fails all three |
| Categories | `Education;Spirituality;` | the registry ties Spirituality to Education |
| Licences | `project_license` Apache-2.0; `metadata_license` CC0-1.0; LICENSE, NOTICE and the font licences installed under `share/licenses/<id>` | NOTICE now names Noto Color Emoji, which every patched build embeds |
| Developer name | `cubancorona`, the copyright holder in LICENSE and NOTICE | AppStream forbids a domain-style name; **open: a personal name or "BibleText" instead** |
| Icon | scaled from `cmd/desktop/Icon.png`, the shipped mark | the same source the Windows tiles now use; `icon/full.png` is the retired dove |
| Brand colours | `#2F4C86` light, `#7CA0E4` dark | the app's own accents (`theme.go`) |
| Screenshots | four window captures under `docs/screenshots/linux/`, referenced by commit-pinned raw URLs; the `<screenshots>` block is left out until `screenshots.ref` names that commit | captured 16 Sep 2026 by `.github/workflows/linux-screenshots.yml`; replace them by dispatching it again (docs/screenshots/README.md) |

**Open for the account holder:** the developer name; the metadata licence
(CC0-1.0 or FSFAP); the NOTICE line for the emoji font (added, factual);
and the Flathub generative-AI disclosure (next section).

## Accounts, names and policies

| Store | What only the account holder can do |
| --- | --- |
| Snap Store | An Ubuntu One account. The name **`bibletext` was REGISTERED on 18 September 2026** through the web console at snapcraft.io/register-snap, which needs no Linux host — the `snapcraft register` CLI is one way to do it, not the only one. The console states the review window as **up to 30 days**, not the two working days recorded here before; until it clears, the snap can be uploaded and its metadata edited but the listing stays PRIVATE, and the privacy control is locked to Private in the form. The credential is now DONE: exported on 18 September 2026 with `snapcraft export-login --snaps bibletext --acls package_access,package_push,package_update,package_release --expires 2027-09-18`, and its content set as the repository secret `SNAPCRAFT_STORE_CREDENTIALS`. It is a scoped, revocable macaroon rather than a password, and it verifies as account `cubancorona` with exactly those four permissions. **Setting it changed what a release does**: `release.yml` gates the publish step on that secret, so the next tag now uploads BOTH the amd64 and the arm64 snap to the `edge` channel by itself instead of leaving them artifacts. Promotion beyond edge is still by hand after a desktop test, and the listing stays Private until the name review clears. The export needs a Linux host because `snapcraft` runs only there; the one used was a local Ubuntu 24.04 arm64 VM. |
| Flathub | A GitHub account with two-factor authentication. Flathub's requirements page (September 2026) asks that AI-generated code, documentation or packaging be disclosed with the affected parts and their extent, and that no AI tool open the PR or write its text or replies: the manifest, the generator and this document were drafted with AI assistance and belong in that disclosure, and the PR, its description and every review reply are written and posted by the account holder. The template also asks for a short video of the Flatpak running (the smoke job records one) and for evidence of development history (the tagged releases, the App Store and Play presence, CI). |
| AppImageHub | A PR to `AppImage/appimage.github.io` adding `data/BibleText` containing the repository URL, after the first release that carries the AppImage. |
| bibletext.co.uk | After Flathub publishes: `docs/org.flathub.VerifiedApps.txt` with the token from the Flathub developer page, plus a copy line and a presence check in `scripts/publish-site.sh` beside the other association files (not written yet). |

## Product identity

| Field | Value |
| --- | --- |
| AppStream id, Flatpak app id, desktop file, icon name | `uk.co.bibletext.BibleText` |
| Desktop entry | `linux/uk.co.bibletext.BibleText.desktop`: `Exec=bibletext %u`, `MimeType=x-scheme-handler/bibletext;`, `StartupWMClass=BibleText` (the toolkit derives the window class from the title, which is the product name) |
| Snap | name `bibletext`, entry `snap/gui/bibletext.desktop` (picked up by name, exported as `bibletext_bibletext.desktop`), the gnome extension plus `network`, `network-bind` (the loopback single-instance listener), `audio-playback`, `home` (Share as image writes to ~/Downloads) |
| AppImage | `BibleText-x86_64.AppImage` and its `.zsync`; update information `gh-releases-zsync|cubancorona|bibletext|latest|BibleText-*x86_64.AppImage.zsync` |
| Version | `cmd/desktop/FyneApp.toml` Version, built from its tag; `linux/releases.toml` must lead with it |
| Where data lives | Flatpak `~/.var/app/uk.co.bibletext.BibleText/{config,cache}`; snap `~/snap/bibletext/<revision>/.config` (backed up per revision, so a revert rolls notes back) and `~/snap/bibletext/common/.cache`; AppImage the XDG defaults like the tarball |
| Flatpak permissions | `--share=ipc`, `--socket=x11` (the toolkit is X11-only, XWayland on Wayland sessions), `--device=dri`, `--socket=pulseaudio`, `--share=network`, `--filesystem=xdg-download` |

## Listing (en-GB)

Summary `Read and study Scripture`. The description is the Windows listing's
opening line, the six features and the closing line, rendered as AppStream
paragraphs and a list, as a snap description with bullets, and as plain
text for AppImageHub; the Flathub variant adds the keyless sentence. The
Play listing's on-device lines (lock-screen narration, read-aloud) are
phone-only and stay out. Keywords: Bible, Scripture, NKJV, Berean, WEB,
BSB, Catholic, deuterocanon, audio, narration, notes, offline.

### Snap Store console fields (not in the yaml)

Categories Books and Reference, Education; up to five gallery images and a
3:1 banner; there is no privacy field, so the description's last line and
the website carry it.

## Per-channel behaviour a reader will notice

- The AppImage does not register `bibletext:` links; a link pasted into
  Search opens as it does everywhere.
- The snap asks "Allow snap bibletext to open file …?" when Share as image
  reveals the saved PNG, once per reveal; a revert restores the older notes.
- Flathub has no NKJV until the reader adds a key; the metainfo says so.
- On a Wayland session all three run under XWayland.

## Pipeline

`.github/workflows/linux-stores.yml` (on demand, and on pushes that touch
the packaging inputs) builds the keyed executable once and then the
AppImage (`scripts/build-appimage.sh`, the entry and the update information
asserted, the executable verified, a headless launch and a second-launch
handoff under Xvfb with software OpenGL) and the snap (`snapcore/action-build`
from the staged binary, installed with `--dangerous`, the exported entry
asserted, the executable verified, the pulse plugin present, the same
smoke); independently, the Flatpak, keyless and from source
(`flatpak-builder` in Flathub's container, the manifest linted — the
metainfo and repository lints wait for the screenshots they require — the
bundle installed, the exported entry asserted, the same smoke, and a short
screen recording for the Flathub PR).
`.github/workflows/release.yml`'s Linux job additionally builds the AppImage
and uploads it with the tarball, and a snap job publishes to the `edge`
channel when the store credential secret exists. Flathub builds its own from
the manifest submitted to it.

The scripts and workflows have not run on a Linux runner yet: the first
`workflow_dispatch` of `linux-stores.yml` is where the vendored-patch
recipe, the flatpak-tagged build, the snap layout and the three smokes are
proven or corrected, exactly as the Windows Store workflow was.

## Order of work

1. Account holder: the open decisions above; register `bibletext` on the
   Snap Store and add the credential secret; decide the Flathub disclosure.
2. Run `linux-stores.yml` by hand and fix what it finds.
3. Screenshots — done: `linux-screenshots.yml` captured the four scenes,
   `screenshots.ref` names their commit, and the metainfo carries them, so
   the Flathub metainfo and repository lints now run.
4. The next release: bump the ledger, add the release to
   `linux/releases.toml` with the tag's date, tag last; the release run
   produces the tarball, the AppImage and its `.zsync`, and the snap on
   `edge`.
5. A Linux desktop for an afternoon (an x86_64 machine or a cloud desktop;
   an ARM virtual machine cannot run these): narration audible in each
   sandbox, the window coming to the front on a `bibletext:` link under
   GNOME and KDE, the browsers' prompts, the snap's file dialog, HiDPI and
   the dark-mode portal in the Flatpak, the FUSE mount on a stock desktop.
6. Publish: promote the snap to `stable`; the Flathub PR (written by the
   account holder) carrying four files at the root of the submission —
   the tag-pinned manifest from
   `go run ./cmd/linuxmeta flatpak-manifest -tag v… -commit …`,
   `flatpak/go.mod.yml`, `flatpak/modules.txt` and `flatpak/flathub.json`;
   the AppImageHub PR, which the first release carrying an AppImage (1.2.10)
   unblocked; and the verification token on the site. The download page links
   the AppImage as of 1.2.10, and `scripts/check-public-surfaces.py` now fails
   CI if a released artifact goes unlinked.
